worker_processes 1;
pid /home/lawbr3aker/Claude/gayroxy/nginx.pid;
events { worker_connections 1024; }
http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    access_log /dev/null;
    error_log /home/lawbr3aker/Claude/gayroxy/logs/nginx-error.log;

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    server {
        listen 9000;
        server_name _;

        # ── Static endpoints ──────────────────────────────────
        location /sub {
            alias /home/lawbr3aker/Claude/gayroxy/sub/subscription.b64;
            default_type text/plain;
        }

        # ── WebSocket locations ────────────────────────────────
        location /5e6588b0598aee68 {
            proxy_pass http://127.0.0.1:10001;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 86400;
            proxy_connect_timeout 86400;
        }

        location /17328c6aa3fbf697 {
            proxy_pass http://127.0.0.1:10002;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 86400;
            proxy_connect_timeout 86400;
        }

        location /da800d9ba3d7bbd0 {
            proxy_pass http://127.0.0.1:10003;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 86400;
            proxy_connect_timeout 86400;
        }

        # ── gRPC locations ────────────────────────────────────
        location /8c24e389d504503a {
            grpc_pass grpc://127.0.0.1:10005;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /3d4264da5f0f9938 {
            grpc_pass grpc://127.0.0.1:10006;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /b3de057df0c668fe {
            proxy_pass http://127.0.0.1:10011;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 86400;
            proxy_connect_timeout 86400;
        }

        location /fb43e1b29f3ef53b {
            grpc_pass grpc://127.0.0.1:10012;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /1bb8c113b9d268cf {
            grpc_pass grpc://127.0.0.1:10013;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # ── HTTPUpgrade locations ─────────────────────────────
        location /5feb6018cb2c0bb8 {
            proxy_pass http://127.0.0.1:10014;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 86400;
            proxy_connect_timeout 86400;
        }

        location /8223dde6b8b9ddd1 {
            proxy_pass http://127.0.0.1:10015;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 86400;
            proxy_connect_timeout 86400;
        }

        location /623dcc283b44060a {
            proxy_pass http://127.0.0.1:10016;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 86400;
            proxy_connect_timeout 86400;
        }

        # ── Geo databases (client-side routing rules) ────────────────
        location /geo/ {
            alias /home/lawbr3aker/Claude/gayroxy/geo/;
            default_type application/octet-stream;
            add_header Cache-Control "public, max-age=3600";
        }

        location /panel {
            root /home/lawbr3aker/Claude/gayroxy/sub;
            try_files $uri /panel.html;
        }

        location / {
            root /home/lawbr3aker/Claude/gayroxy/sub;
            try_files $uri /index.html;
        }
    }
}
