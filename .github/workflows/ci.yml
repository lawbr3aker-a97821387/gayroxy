name: CI

on:
  pull_request:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck all scripts
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq shellcheck
          while IFS= read -r -d '' f; do
            shellcheck -x --severity=warning "$f" || exit 1
          done < <(find scripts -name '*.sh' -type f -print0)

  shfmt:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: shfmt check (advisory)
        run: |
          # Prebuilt binary from official releases (no apt package guarantees)
          curl -sSL -o /usr/local/bin/shfmt \
            "https://github.com/mvdan/sh/releases/download/v3.14.0/shfmt_v3.14.0_linux_amd64"
          chmod +x /usr/local/bin/shfmt
          # Advisory: scripts intentionally follow compact legacy formatting
          # (derived VERBATIM from proxy.sh). Fail soft, never block the deploy.
          shfmt -d -i 2 -ci -bn scripts/ tools/ 2>/dev/null | grep -q . \
            && echo "shfmt: formatting differences (non-blocking)" \
            || echo "shfmt clean"

  actionlint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: actionlint
        run: |
          go install github.com/rhysd/actionlint/cmd/actionlint@latest
          # go install lands in ~/go/bin which isn't on the runner PATH by default
          export PATH="$HOME/go/bin:$PATH"
          actionlint

  render-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Bash syntax check all scripts
        run: |
          while IFS= read -r -d '' f; do
            bash -n "$f" || echo "SYNTAX FAIL: $f"
          done < <(find scripts -name '*.sh' -type f -print0)

  worker-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Node syntax check worker
        run: |
          node --check worker/index.js